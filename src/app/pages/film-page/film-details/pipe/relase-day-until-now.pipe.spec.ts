import { RelaseDayUntilNowPipe } from './relase-day-until-now.pipe';

describe('RelaseDayUntilNowPipe', () => {
  it('create an instance', () => {
    const pipe = new RelaseDayUntilNowPipe();
    expect(pipe).toBeTruthy();
  });
});
